// Room: /d/wudujiao/duchuan.c

inherit ROOM;

void create()
{
        set("short", "渡船");
        set("long", @LONG
一叶小舟，最多也就能载七、八个人。一名六十多岁的老艄公
手持长竹篙，正在船尾吃力地撑着船。
LONG);
        set("outdoors", "wudujiao");
	set("no_clean_up", 0);
	set("coor/x", -45500);
	set("coor/y", -81010);
	set("coor/z", 0);
	setup();
        replace_program(ROOM);
}