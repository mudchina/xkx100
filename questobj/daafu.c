// daafu.c

inherit ITEM;

void create()
{
	set_name("大阿福", ({"da afu", "afu"}));
	set("taskobj", 1);
	set("long", "这是一尊憨态可掬的大阿福。这尊大阿福塑成男像。\n");
	set("unit", "尊");
	set("weight", 10);
	set("value", 0);
}

